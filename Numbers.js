const num1= 100;
const num2=50;

let val;

val= num1 + num2;
val= num1 * num2;
val = num1 - num2;
val= num1 / num2;


// Math Object

val= Math.PI;
val= Math.E;
val=Math.round(102.);
val=Math.ceil(2.4);
val=Math.floor(2.9);
val=Math.sqrt('25');
val=Math.abs(-3); // gives the positive number
val=Math.pow(2,3);
val=Math.min(1,2,3100,230,0,-2);
val=Math.max(1000,1,2,3,4,5);
val=Math.random();

// get the random number from 1 to 20
val = Math.random() *20 +1;

// Avoid decimal

val = Math.floor(Math.random() * 20 +1);
console.log(val);

